# API URL - Only needed when running frontend separately from backend
# For integrated setup, use relative URLs instead
VITE_API_URL=http://localhost:3000

# GitHub Client ID - Only needed if frontend needs direct GitHub API access
# VITE_GITHUB_CLIENT_ID=your_github_client_id
